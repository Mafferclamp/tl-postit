<% header = link_to("All Posts", posts_path)+" &raquo; #{@categories.name}".html_safe%>

<%= render 'shared/content_title', title: header %>

<% @categories.posts.each do |post| %>
	<%= render 'posts/post', post: post %>
<% end %>